# TODO

- Confirmar normativa interna exacta para:
  - cupo diario
  - máximo anual de días por docente
  - desempate por letra
  - bloqueos obligatorios frente a simple revisión manual
- Completar calendario real del curso académico.
- Decidir si se activarán adjuntos y, si es así, definir:
  - tipos permitidos
  - tamaño máximo
  - escaneo
  - retención
- Validar en pruebas reales la estrategia elegida para AppSheet:
  - tablas base para escritura simple
  - `config.parametro_sistema` como tabla base de configuración editable
  - `appsheet.vw_revision_solicitudes` como vista de solo lectura
- Definir estrategia de autenticación real de usuarios.
- Definir política de copia de seguridad, retención y borrado.
